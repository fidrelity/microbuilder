---
applications:
  .:
    name: microbuilder
    url: ${name}.${target-base}
    framework:
      name: rails3
      info:
        mem: 256M
        description: Rails Application
        exec: 
    mem: 256M
    instances: 1
    services:
      mysql-61bf3:
        type: mysql

#gallery

  .centered_content
    .label
      Gallery
    %ul{:class=>"gamelist"}
      - @games.each do |game|
  
  
        %li{:id => game.id, :class => "game"}
          %span
            = link_to game.title, play_path(game)
            = "by #{game.author.display_name}"
#content

:javascript
  $(document).ready( function() {
    
    var data = null;
    var name = "#{current_user ? current_user.display_name : ""}";
    paint_main( data, name );
    
  } );

.loadOverlay
  %div{:class => "overlayMessage row"}
    %div{:class => "span1"}
      =image_tag "loader_circle.gif"
    %div{:class => "span6"}
      %div{ :class => "span6 message"}

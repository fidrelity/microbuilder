#create
  .banner
    =image_tag "banner_editor.png"
    Paint

  -if user_signed_in?      
    =render :partial => "paint_tool"
  -else
    =render :partial => "paint_guide"

.paintOverlay
  %div{:class => "paintOverlayMessage row"}
    %div{:class => "span1"}
      =image_tag "loader_circle.gif"
    %div{:class => "span6"}
      %div{ :class => "span6 message"}
  

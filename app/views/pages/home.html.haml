%div{ :class => "hero-unit", :style => "background:#222;color:#FFF;clear:both;"}
  %h2
    Play and Build Microgames*
  %p
    Ever wanted to build your own games and share it with your friends? Here you can! Paint you own graphics, make your game and release it!
    All you need is your browser and imagination!
  
  %p
    =link_to "Start building now", pages_editor_path, { :class => "btn btn-primary btn-large"}
    
  %small* no programming skills required.

%div{ :class => "row"}
  %div{ :class => "span8"}    
    %h2
      Latest games:

    %ul{:class => "thumbnails"}
      - @games.each do |game|
        %li{:id => game.id, :class => "span2"}
          %div{:class => "thumbnail"}
            = link_to image_tag("game.png"), play_path(game)
            %h5
              =link_to "by #{game.author.display_name}", user_path(game.author)

  %div{ :class => "span4"}
    %h2
      Login:

    %div{ :class => "well"}

      %label
        E-Mail
      %input{ :type=>"text", :class=>"span3", :placeholder=>"E-Mail" }

      %label
        Password
      %input{ :type=>"password", :class=>"span3", :placeholder=>"Password" }

      %button{:type=>"submit", :class=>"btn"}
        Login

      =link_to 'Facebook Login', user_omniauth_authorize_path(:facebook), :class=>"btn btn-primary"
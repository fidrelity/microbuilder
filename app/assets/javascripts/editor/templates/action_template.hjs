{{dynamicView App.actionController.contentView}}

{{#if App.actionController.showSaveButton}}

  <p>Is your {{App.actionController.mode}} correct?</p>

  <p>
    {{App.actionController.action.string}} 
    <button class="btn" {{action "save" target="App.actionController"}}> save </button>
  </p>

{{/if}}

<button class="btn" {{action "showObjects" target="App.gameController"}}> cancel </button>

<div class="editor_content">

  <h2>Game Editor</h2>

  <button {{action "showStage"}}> stage </button>
  <button {{action "showPaint"}}> paint </button>

  {{dynamicView editorContent}}
  
  {{#if overlayContent}}
  
    <div id="overlay">
    
      <h2>Overlay</h2>
      <button {{action "hideOverlay"}}> close </button>
    
      {{dynamicView overlayContent}}
    
    </div>
  
  {{/if}}

</div>

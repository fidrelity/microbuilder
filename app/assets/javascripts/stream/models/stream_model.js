var StreamModel = Ember.Object.extend({
  
  
  
});

// -------------------------------------------

var GameActivityModel = StreamModel.extend({
  
  templateName: 'stream/templates/create_game_template',

});